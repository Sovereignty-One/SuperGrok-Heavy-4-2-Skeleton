services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    # Internal only — accessed via node-bridge proxy on 9899
    expose:
      - "9897"
    environment:
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis

  frontend:
    build:
      context: ./supergrok-enterprise
      dockerfile: Dockerfile
    ports:
      - "9899:80"
    environment:
      - API_URL=http://backend:9897
    depends_on:
      - backend

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    ports:
      - "8080:8080"
      - "8443:8443"
    environment:
      - KEYCLOAK_ADMIN=${KEYCLOAK_ADMIN:-admin}
      - KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD:-changeme}
    command: start-dev
    depends_on:
      - redis

  redis:
    image: redis:7
    # Internal only — no external exposure needed
    expose:
      - "6379"
